---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "cert-manager"
  namespace: core
spec:
  project: default
  source:
    repoURL: "https://github.com/Ornias1993/homelab-kube-cluster.git"
    path: manifests/core/02-cert-manager
    targetRevision: main
  destination:
    server: "https://kubernetes.default.svc"
    namespace: core
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
