---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "traefik"
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/Ornias1993/homelab-kube-cluster.git"
    path: manifests/core/04-traefik
    targetRevision: main
  destination:
    server: "https://kubernetes.default.svc"
    namespace: core
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
