---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "k8s-gateway"
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/Ornias1993/homelab-kube-cluster.git"
    path: manifests/core/05-k8s-gateway
    targetRevision: main
  destination:
    server: "https://kubernetes.default.svc"
    namespace: core
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
